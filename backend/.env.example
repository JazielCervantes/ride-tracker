# Base de datos (Railway provee DATABASE_URL automáticamente)
DATABASE_URL=mysql+pymysql://user:password@host:3306/ride_tracker

# JWT
SECRET_KEY=cambia_esto_por_un_secreto_muy_largo_y_aleatorio
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_DAYS=7

# App
DEBUG=False
PRODUCTION=True

# CORS — URL del frontend en Vercel
ALLOWED_ORIGINS=https://ride-tracker.vercel.app
